name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag del release (ej: v2.3.0). Debe existir en el repo.'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: linux-x64
            artifact: easy-commit-linux-x64
          - os: ubuntu-latest
            target: linux-arm64
            artifact: easy-commit-linux-arm64
          - os: macos-latest
            target: darwin-x64
            artifact: easy-commit-darwin-x64
          - os: macos-latest
            target: darwin-arm64
            artifact: easy-commit-darwin-arm64
          - os: windows-latest
            target: windows-x64
            artifact: easy-commit-windows-x64.exe

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run tests
        run: bun test

      - name: Build standalone binary
        run: bun run build:standalone
        env:
          VERSION: ${{ github.event.inputs.tag }}
          BUILT_BY: github-actions

      - name: Rename binary (Unix)
        if: runner.os != 'Windows'
        run: mv easy-commit ${{ matrix.artifact }}

      - name: Rename binary (Windows)
        if: runner.os == 'Windows'
        run: mv easy-commit.exe ${{ matrix.artifact }}

      - name: Sign binary (macOS)
        if: runner.os == 'macOS'
        run: |
          xattr -cr ${{ matrix.artifact }}
          codesign --sign - --force ${{ matrix.artifact }}

      - name: Create archive (Unix)
        if: runner.os != 'Windows'
        run: |
          chmod +x ${{ matrix.artifact }}
          tar czf ${{ matrix.artifact }}.tar.gz ${{ matrix.artifact }} README.md LICENSE.md

      - name: Create archive (Windows)
        if: runner.os == 'Windows'
        run: |
          7z a -tzip ${{ matrix.artifact }}.zip ${{ matrix.artifact }} README.md LICENSE.md

      - name: Generate SHA256 checksum (Unix)
        if: runner.os != 'Windows'
        run: |
          if [ "${{ runner.os }}" = "macOS" ]; then
            shasum -a 256 ${{ matrix.artifact }}.tar.gz > ${{ matrix.artifact }}.tar.gz.sha256
          else
            sha256sum ${{ matrix.artifact }}.tar.gz > ${{ matrix.artifact }}.tar.gz.sha256
          fi

      - name: Generate SHA256 checksum (Windows)
        if: runner.os == 'Windows'
        run: |
          certutil -hashfile ${{ matrix.artifact }}.zip SHA256 > ${{ matrix.artifact }}.zip.sha256

      - name: Upload artifacts (Unix)
        if: runner.os != 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            ${{ matrix.artifact }}.tar.gz
            ${{ matrix.artifact }}.tar.gz.sha256
          retention-days: 7

      - name: Upload artifacts (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            ${{ matrix.artifact }}.zip
            ${{ matrix.artifact }}.zip.sha256
          retention-days: 7

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.tag }}

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release
          pattern: easy-commit-*
          merge-multiple: true

      - name: Display structure of downloaded files
        run: ls -R release

      - name: Prepare release files
        run: |
          ls -lh release/

      - name: Generate changelog
        id: changelog
        run: |
          echo "## What's Changed" > CHANGELOG.md
          git log $(git describe --tags --abbrev=0 ${{ github.event.inputs.tag }}^)...${{ github.event.inputs.tag }} --pretty=format:"- %s (%h)" >> CHANGELOG.md || \
          git log --pretty=format:"- %s (%h)" -20 >> CHANGELOG.md
          cat CHANGELOG.md

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag }}
          files: release/*
          body_path: CHANGELOG.md
          draft: false
          prerelease: ${{ contains(github.event.inputs.tag, 'alpha') || contains(github.event.inputs.tag, 'beta') || contains(github.event.inputs.tag, 'rc') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
